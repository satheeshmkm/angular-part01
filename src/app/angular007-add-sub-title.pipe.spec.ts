import { Angular007AddSubTitlePipe } from './angular007-add-sub-title.pipe';

describe('Angular007AddSubTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new Angular007AddSubTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
